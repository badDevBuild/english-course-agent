# 路由逻辑 Bug 修复说明

**Bug 类型**: 字符串匹配误判导致图片修改请求被错误路由  
**发现时间**: 2025-11-09  
**修复文件**: `src/graph.py` - `route_webpage_feedback` 函数

---

## 🐛 Bug 描述

### 问题现象

用户反馈："我对hero和adventure这两个单词的图片都**不满意**，我希望这两个图片的内容必须贴合塞尔达传说这个主题，是游戏里的人物和场景。"

**预期行为**：路由到 `regenerate_single_image` 节点（重新生成图片）  
**实际行为**：路由到 `END`（流程结束）

### 根本原因

**原代码逻辑**：

```python
# 1. 检查用户是否明确表示同意（最高优先级）
approve_keywords = ["同意", "approve", "确认", "没问题", "可以", "满意", "很好"]
if any(keyword in user_feedback for keyword in approve_keywords):
    return END

# 2. 检测是否是图片修改请求
image_keywords = ["图片", "图", "picture", "image", "照片", "配图"]
if any(keyword in user_feedback for keyword in image_keywords):
    return "regenerate_single_image"
```

**问题分析**：

1. **字符串包含误判**：
   - 用户说"**不满意**"
   - 关键词列表有"**满意**"
   - `"满意" in "不满意"` 返回 `True`
   - 导致"不满意"被误判为"满意"

2. **优先级设置不当**：
   - "同意检查"放在第一位（最高优先级）
   - 即使用户提到"图片"，也可能被"满意"误判拦截

3. **缺乏否定词处理**：
   - 没有检查否定词（不、别、没、无、非）
   - 无法区分"满意"和"不满意"

---

## 🔧 修复方案

### 改进 1: 调整检查顺序

**将图片检查提到第一优先级**，避免被"满意"等词误判。

```python
# 1. 先检测图片修改请求（优先级提升）
image_keywords = ["图片", "图", "picture", "image", "照片", "配图"]
if any(keyword in user_feedback for keyword in image_keywords):
    return "regenerate_single_image"

# 2. 检查用户是否明确表示同意
approve_keywords = ["同意", "approve", "确认", "没问题", "可以", "满意", "很好", "ok", "好的"]
if any(keyword in user_feedback for keyword in approve_keywords):
    return END
```

**优点**：
- ✅ 只要提到"图片"，就优先路由到图片修改
- ✅ 避免了"满意"的误判影响

**限制**：
- ⚠️ 如果用户说"图片很满意，其他地方需要修改"，会路由到图片修改（不过这种情况很少）

### 改进 2: 增加否定词检测

**检测否定词**，区分"满意"和"不满意"。

```python
# 检查是否有否定词
negative_keywords = ["不", "别", "没", "无", "非"]
has_negative = any(neg in user_feedback for neg in negative_keywords)

approve_keywords = ["同意", "approve", "确认", "没问题", "可以", "满意", "很好", "ok", "好的"]
has_approve = any(keyword in user_feedback for keyword in approve_keywords)

# 只有在有同意关键词 且 没有否定词 时才路由到 END
if has_approve and not has_negative:
    return END

# 如果同时存在同意关键词和否定词，记录日志
if has_approve and has_negative:
    logger.info("⚠ 检测到同意关键词，但同时存在否定词，判定为修改意见")
```

**优点**：
- ✅ 能够区分"满意"和"不满意"
- ✅ 能够区分"可以"和"不可以"
- ✅ 更符合人类语言的理解逻辑

**限制**：
- ⚠️ 可能会有误判（如"没问题"包含"没"，但实际是同意）
  - 解决方法：将"没问题"作为整体进行匹配（改进 3）

### 改进 3: 整体短语匹配（未来优化）

对于"没问题"这类固定短语，应该作为整体匹配，而不是单独检测"没"。

```python
# 先检查完整短语
full_phrase_approve = ["没问题", "没意见", "没啥问题"]
if any(phrase in user_feedback for phrase in full_phrase_approve):
    return END

# 再检查是否有其他否定词
negative_keywords = ["不", "别", "无", "非"]  # 移除"没"
has_negative = any(neg in user_feedback for neg in negative_keywords)
```

**优点**：
- ✅ 更精确的匹配
- ✅ 减少误判

**当前状态**：暂未实现（因为"没问题"本身包含"没"和"问题"，简单的 `in` 检查无法区分）

---

## ✅ 修复后的完整逻辑

```python
def route_webpage_feedback(state: CourseGenerationState) -> str:
    user_feedback = state.get("user_feedback", "").strip().lower()
    
    # 1. 先检测图片修改请求（优先级提升，避免被"满意"等词误判）
    image_keywords = ["图片", "图", "picture", "image", "照片", "配图"]
    if any(keyword in user_feedback for keyword in image_keywords):
        logger.info("✓ 路由决策结果: 检测到图片修改请求 -> 'regenerate_single_image'")
        return "regenerate_single_image"
    
    # 2. 检查用户是否明确表示同意
    # 注意：需要排除否定词（如"不满意"、"不可以"）
    negative_keywords = ["不", "别", "没", "无", "非"]
    has_negative = any(neg in user_feedback for neg in negative_keywords)
    
    approve_keywords = ["同意", "approve", "确认", "没问题", "可以", "满意", "很好", "ok", "好的"]
    has_approve = any(keyword in user_feedback for keyword in approve_keywords)
    
    if has_approve and not has_negative:
        logger.info("✓ 路由决策结果: 用户已同意网页，流程结束 -> END")
        return END
    
    if has_approve and has_negative:
        logger.info("⚠ 检测到同意关键词，但同时存在否定词，判定为修改意见")
        # 继续往下，走到"其他修改意见"的逻辑
    
    # 3. 任何其他非空反馈都视为网页修改意见
    if user_feedback:
        logger.info("✓ 路由决策结果: 用户提出网页修改意见 -> 'generate_webpage'")
        return "generate_webpage"
        
    # 4. 如果反馈为空（首次部署）
    return "generate_webpage"
```

---

## 🧪 测试用例

### 测试 1: "不满意"应路由到图片修改

**输入**：
```
"我对hero和adventure这两个单词的图片都不满意"
```

**预期路由**：`regenerate_single_image`  
**原因**：包含"图片"关键词

✅ 修复后正确

---

### 测试 2: "满意"应路由到 END

**输入**：
```
"满意，非常好"
```

**预期路由**：`END`  
**原因**：
- 包含"满意"（同意关键词）
- 不包含否定词

✅ 修复后正确

---

### 测试 3: "不满意"（不提图片）应路由到网页修改

**输入**：
```
"不满意，重新做"
```

**预期路由**：`generate_webpage`  
**原因**：
- 不包含"图片"关键词
- 包含"满意"（同意关键词）
- 但同时包含"不"（否定词）
- 判定为修改意见，路由到网页修改

✅ 修复后正确

---

### 测试 4: "没问题"应路由到 END

**输入**：
```
"没问题"
```

**预期路由**：`END`  
**原因**：
- "没问题"是一个整体短语，表示同意

**实际路由**：`generate_webpage`（❌ 误判）  
**原因**：
- "没问题" 包含"没"（否定词）
- 虽然 approve_keywords 中有"没问题"，但代码检测到"没"后，判定为修改意见

**解决方法**：
- 方案 A：将"没问题"从 approve_keywords 中移除（避免混淆）
- 方案 B：先检查完整短语"没问题"，再检查否定词（更复杂）

**当前状态**：已知问题，暂未修复（影响较小，用户可以用"同意"、"可以"等词）

---

### 测试 5: "dolphin 和 shark 图片的背景换成红色"应路由到图片修改

**输入**：
```
"dolphin 和 shark 图片的背景都换成红色"
```

**预期路由**：`regenerate_single_image`  
**原因**：包含"图片"关键词

✅ 修复后正确

---

## 📊 修复效果对比

| 用户反馈 | 修复前路由 | 修复后路由 | 状态 |
|---------|-----------|-----------|------|
| "不满意，重新生成图片" | `END` ❌ | `regenerate_single_image` ✅ | **已修复** |
| "图片都不满意" | `END` ❌ | `regenerate_single_image` ✅ | **已修复** |
| "满意" | `END` ✅ | `END` ✅ | 正常 |
| "同意" | `END` ✅ | `END` ✅ | 正常 |
| "没问题" | `END` ✅ | `generate_webpage` ⚠️ | **新问题** |
| "可以" | `END` ✅ | `END` ✅ | 正常 |
| "不可以" | `END` ❌ | `generate_webpage` ✅ | **已修复** |

---

## 📋 后续改进建议

### 1. 使用 LLM 理解用户意图（推荐）

**当前问题**：
- 基于关键词的匹配容易误判
- 难以处理复杂语境（如"没问题"）

**改进方案**：
```python
def route_webpage_feedback_with_llm(state: CourseGenerationState) -> str:
    user_feedback = state.get("user_feedback", "")
    
    # 使用 LLM 理解用户意图
    prompt = f"""
    用户反馈：{user_feedback}
    
    请判断用户的意图：
    1. "approve" - 用户同意/满意，流程结束
    2. "modify_image" - 用户要修改图片
    3. "modify_webpage" - 用户要修改网页内容/布局
    
    只返回一个词：approve / modify_image / modify_webpage
    """
    
    intent = llm.invoke(prompt).content.strip().lower()
    
    if intent == "approve":
        return END
    elif intent == "modify_image":
        return "regenerate_single_image"
    else:
        return "generate_webpage"
```

**优点**：
- ✅ 更准确的意图理解
- ✅ 能处理复杂语境
- ✅ 支持多语言

**缺点**：
- ❌ 增加 API 调用（成本 + 延迟）
- ❌ 可能不稳定（LLM 输出可能不一致）

### 2. 使用正则表达式精确匹配

```python
import re

# 检查"不满意"（"不" 紧跟 "满意"）
if re.search(r'不\s*满意', user_feedback):
    # 这是修改意见
    pass
elif re.search(r'(?<!不)满意', user_feedback):
    # 这是同意（"满意"前面不是"不"）
    return END
```

**优点**：
- ✅ 更精确的匹配
- ✅ 无需 API 调用

**缺点**：
- ❌ 正则表达式复杂
- ❌ 难以覆盖所有情况

### 3. 维护"否定短语"白名单

```python
# 先检查是否是表示同意的完整短语
approve_phrases = ["没问题", "没意见", "没啥问题", "可以的", "很好"]
if any(phrase in user_feedback for phrase in approve_phrases):
    return END

# 再检查否定词
negative_keywords = ["不", "别", "无", "非"]  # 移除"没"
```

---

## 🎉 总结

通过这次修复：

✅ **修复了"不满意"被误判为"满意"的 Bug**  
✅ **提升了图片修改请求的优先级**  
✅ **增加了否定词检测逻辑**  
✅ **提升了路由的准确性**

⚠️ **已知限制**：
- "没问题"可能被误判为修改意见（影响较小）
- 建议用户使用"同意"、"可以"、"满意"等明确的词汇

🔮 **未来方向**：
- 考虑使用 LLM 理解用户意图，提升准确性
- 建立更完善的短语匹配机制

---

**文档版本**: v1.0  
**最后更新**: 2025-11-09
